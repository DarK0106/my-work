새 프로젝트
- "BasicTest" -> "com.test.java"

STS3 + Spring MVC Project Template
- 구형에서 최신 버전으로 바꾸자
- Spring Framework

Spring Framework 1.0 ~ 7.0
- Spring Framework 5.0 ~ 6.0 을 주로 사용

Spring Framework -> 5.x
스프링 위에서 스프링 부트가 돌아감
Spring Boot -> 3.x

pom.xml에서 버전 바꾸면됨

Spring MVC Project 구조

BasicTest -> pom.xml(Project Object Nodel, 메이븐 설정 파일)
			- lombok 설정
메이븐, Maven
- 프로젝트 관리 도구(따로 설치 안해도 이클립스에 포함되어있음)
- 프로젝트를 처음에 만들어주거나 만든 프로그램을 지속적으로 관리를
해주거나 아니면 만들어진걸 어딘가에 배포 하는 작업 전반을 도와줌
-우리가 사용할때의 메이븐의 주 목적은 의존성 관리 -> 프로젝트에 필요한
*.jar 관리

main -> 개발 자원이 있는 폴더
src/main/java
	- *.java 파일 보관
src/main/resources
	- 자바 이외의 파일들 보관

test -> 테스트용 자원이 있는 폴더: 단위 테스트?
src/test/java
src/test/resources

JRE System Library -> JDK(JRE)
Maven Dependencies -> 추가 라이브러리

src -> main -> webapp: 웹 루프 폴더

- webapp -> resources: 이전의 asset 폴더 역할(js, css, images 등)
- webapp -> WEB-INF
- webapp -> WEB-INF -> classes: *.class 저장(개발자가 접근할 일이 별로 없음)
- webapp -> WEB-INF -> spring: 스프링 설정 폴더
- webapp -> WEB-INF -> spring -> root-context.xml
- webapp -> WEB-INF -> spring -> appServlet -> servlet-context.xml
- webapp -> WEB-INF -> views: View 폴더(JSP 페이지)
- webapp -> WEB-INF -> web.xml: 톰캣 설정

스프링 주요 이론
1. DI(IoC)
2. A0P

- src/main/java -> "com.test.java.di01"
				   		- "Main.java"
				   		- "Service.java"
				   		- "Hong.java"
				   		- "Lee.java"
				   		
- src/main/java -> "com.test.java.di02"
				   		- "Main.java"
				   		- "Service.java"
				   		- "Employee.java"(I)
				   		- "Hong.java"
				   		- "Lee.java"
				   		
- src/main/java -> "com.test.java.di03"
				   		- "Main.java"
				   		- "Service.java"
				   		- "Employee.java"(I)
				   		- "Hong.java"
				   		- "Lee.java"
				   		- "di03.xml" // 스프링 설정 파일
				   		
Spring에서 여러가지 설정을 하는데 설정하는 방식이 3종류가 있다
1. XML을 통한 설정 -> Spring

2. Annotation을 통한 설정 -> Spring, Spring Boot

3. Java를 통한 설정 -> Spring Boot

AOP
메모라는 주 업무 구현
- "com.test.java.aop"
			- "Main.java"
			- "Memo.java"(I)
			- "MemoImpl.java"(C) -> 메모 객체 -> 주 업무
			- "Logger.java" -> 로그 객체 -> 보조 업무
			- "memo.xml" 스프링 설정 파일
			
인터페이스와 그걸 구현하는 구현 클래스
인터페이스 이름: IMemo, Memo
구현 클래스: Memo, MemoImpl

새 프로젝트
- Spring Legacy Project -> Spring MVC Project -> "WebTest"
-> "com.test.java"
여기서 루트 컨텍스트 이름은 STS가 자동으로 java로 설정함
-> 문제: 루트 컨텍스트 이름 중복되면 톰캣 에러(톰캣 -> Modules)