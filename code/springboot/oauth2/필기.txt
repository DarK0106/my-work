프로젝트 생성
- Name: "oauth2" (확인 필요)
- Language: Java
- Type: Gradle - Groovy
- Group: "com.test"
- Artifact: "jsp"
- Package name: "com.test.java"
- Java: 21
- Packaging: jar
- Dependencies: (확인 필요)
    - Spring Web
    - Lombok
    - Oracle Driver <- DB 접속 시 필요(JDBC)
    - Thymeleaf
    - Spring Boot DevTools // 파일 수정 -> 프로젝트 자동 새로고침
    - Spring Data JPA <- MyBatis를 대체하기 위한 의존성, MyBatis와 같이 쓰기도 함
    - Spring Security
    - Oauth2 Client
    
    
1. 애플리케이션 시작
    - 에러 발생
    - build.gradle 로 가서 DB 쪽 잠시 주석

2. application.yml
    - 기본 설정
    
3. 파일 생성하기
    
4. 시큐리티 설정하기
    - com.test.java.config -> SecurityConfig.java
    
OAuth2
- 사용자가 자신의 계정 정보(아이디/암호)를 직접 공유하지 않고, 제 3자 애플리케이션이 사용자의 특정 리소스에 접근할 수 있도록 권한을 부여하는 개방형 표준 프로토콜
- 사이트 구축 후 


인증 필터
처음 요청하는 로그인 시도 URL과 인증 서버로부터 코드를 반환받아 돌아오는 URL
    - OAuth2AuthorizationRequestRedirectFilter
        - /oauth2/authorization/서비스명
        - /oauth2/authorization/naver
        - /oauth2/authorization/kakao
        - /oauth2/authorization/google
        
인증 서버 -> 코드를 반환받아 돌아오는 URL
    - OAuth2LoginAuthenticationFilter
        - /login/oauth2/code/서비스명
        - /login/oauth2/code/naver
        - /login/oauth2/code/kakao
        - /login/oauth2/code/google
        
구글로 소셜 로그인 해보자 !!!!!!!!!

구글로 소셜 로그인 하는걸 신청해야한다?
google cloud console 검색

소셜 로그인 설정 + 사이트 설정
- application.yml

OAuth2 관련 작업
SecurityConfig.java -> OAuth2 처리 추가
com.test.java.dto -> UserDto.java
                  -> CustomOAuth2User.java // CustomUserDetails 인증객체
                  -> GoogleResponse.java // 구글에 반환하는 정보 객체
                  
com.test.java.entity -> User.java
com.test.java.repository -> UserRepository.java(Interface)

// CustomUserDetailsService 역할
com.test.java.service -> CustomAuth2UserService.java

script.sql

SecurityConfig.java -> 로그인 설정 추가

현재 작업 + DB 연동
- 처음: 시작 -> 로그인 -> 구글 아이콘 클릭 -> 구글 인증 -> (개입) -> 로그인 완료
    1. DB 조회
    2. 개인 정보 입력 페이지
    3. DB 추가
    
- 재방문: 시작 -> 로그인 -> 구글 아이콘 클릭 -> 구글 인증 -> (개입) -> 로그인 완료
    1. DB 조회

com.test.java.service -> CustomOAuth2LoginSuccessHandler.java 
<- 인증받고 난 뒤에 우리가 하고싶은 일을
하기 위한 핸들러, Bean 이어야 함. service 패키지에 만들긴 했는데
서비스는 아님